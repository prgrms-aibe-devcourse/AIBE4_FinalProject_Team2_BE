spring:
  datasource:
    url: jdbc:mysql://mysql:3306/${MYSQL_DATABASE:aibe}
    username: ${MYSQL_USER:aibe}
    password: ${MYSQL_PASSWORD:aibe1234}

  data:
    redis:
      host: redis
      port: 6379

  jpa:
    hibernate:
      ddl-auto: validate
